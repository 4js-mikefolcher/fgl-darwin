fglform region.per
if [ $? -eq 0 ]; then
   mv region.42f ../bin/.
else
   exit -1
fi

fgl2p -o main_region.42r main_region.4gl main_lib.4gl region.4gl
if [ $? -eq 0 ]; then
   mv main_region.42r main_region.42m main_lib.42m region.42m ../bin/.
else
   exit -1
fi
