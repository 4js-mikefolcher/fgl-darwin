fglform employees.per
if [ $? -eq 0 ]; then
   mv employees.42f ../bin/.
else
   exit -1
fi

fgl2p -o main_employees.42r main_employees.4gl main_lib.4gl employees.4gl
if [ $? -eq 0 ]; then
   mv main_employees.42r main_employees.42m main_lib.42m employees.42m ../bin/.
else
   exit -1
fi
